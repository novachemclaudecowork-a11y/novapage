{
  "name": "novananoinks-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run redirects && astro dev",
    "prebuild": "node scripts/build-redirects.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "redirects": "node scripts/build-redirects.mjs",
    "test": "node scripts/check_node.mjs && node scripts/test_access.mjs && node scripts/test_auth.mjs && node scripts/test_redirects.mjs",
    "check:node": "node scripts/check_node.mjs",
    "check": "astro check",
    "admin:password": "node scripts/make-admin-password.mjs",
    "test:ui": "node scripts/test_admin_ui.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.3"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
