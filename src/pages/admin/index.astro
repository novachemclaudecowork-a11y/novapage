---
import '../../styles/admin.css';
// 前台的樣式原封不動帶進來，給最新訊息的即時預覽用。
// 預覽放在 sandbox iframe 裡，直接套用同一份 CSS，看到的就是網站上的樣子；
// 不能用 /_astro/... 的路徑，那個檔名每次建置都會變。
import siteCss from '../../styles/global.css?raw';

// 後台主畫面。
// 只有通過驗證的請求才看得到這個頁面，驗證在 worker/index.js 執行。
// 實際的操作邏輯在 src/scripts/admin.js。
---

<!doctype html>
<html lang="zh-Hant-TW">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>網站後台｜貝星貿易</title>
  </head>
  <body>
    <header class="bar">
      <h1>貝星貿易　網站後台</h1>
      <span class="status" id="status"></span>
      <span class="spacer"></span>
      <button class="btn" type="button" id="save" disabled>儲存</button>
      <button class="btn ghost" type="button" id="logout">登出</button>
    </header>

    <nav class="tabs" aria-label="後台功能">
      <button type="button" data-tab="products" aria-selected="true">產品</button>
      <button type="button" data-tab="categories" aria-selected="false">分類</button>
      <button type="button" data-tab="news" aria-selected="false">最新訊息</button>
      <button type="button" data-tab="about" aria-selected="false">公司簡介</button>
    </nav>

    <main>
      <p class="note" id="notice" hidden></p>
      <p class="note" id="session-notice" hidden></p>

      <p class="note">
        修改後請按右上角的<strong>儲存</strong>。儲存會送到 GitHub 並觸發網站重新建置，
        <strong>約一兩分鐘後</strong>正式網站才會看到變更。
        每次儲存都有完整紀錄，改錯了可以還原。
      </p>

      <div id="content">
        <p class="empty"><span class="spin">◐</span> 載入中…</p>
      </div>
    </main>

    <!-- type 不是 module/javascript，瀏覽器不會執行，只是拿來存放預覽用的樣式 -->
    <script type="text/css" id="preview-css" is:inline set:html={siteCss}></script>

    <script src="../../scripts/admin.js"></script>
  </body>
</html>
